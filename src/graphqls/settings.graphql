query historyList {
  versions {
    collection {
      id
      description
      createdAt
    }
  }
}

mutation destroyHistory($input: DestroyVersionInput!) {
  destroyVersion(input: $input) {
    clientMutationId
  }
}
