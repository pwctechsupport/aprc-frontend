query itSystems($filter: BaseScalar = {}, $page: Int, $limit: Int){
  itSystems(filter: $filter, page: $page, limit: $limit) {
    collection {
      id
      name
    }
    metadata {
      currentPage
      limitValue
      totalCount
      totalPages
    }
  }
}