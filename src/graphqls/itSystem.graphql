query itSystems($filter: BaseScalar = {}, $page: Int, $limit: Int = 25) {
  itSystems(filter: $filter, page: $page, limit: $limit) {
    collection {
      id
      name
    }
    metadata {
      currentPage
      limitValue
      totalCount
      totalPages
    }
  }
}
